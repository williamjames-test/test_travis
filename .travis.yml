language: python
install: skip

jobs:
  include:
    - dist: focal
      os: linux
      script:
      - echo "Threads/core $(nproc --all)"
      - free -g
    - dist: focal
      os: linux
      script:
      - echo "Threads/core $(nproc --all)"
      - free -g


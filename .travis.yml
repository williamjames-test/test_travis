language: python
install: skip

jobs:
  include:
    - dist: focal
      os: linux
      arch: ppc64le
      vm:
        size: medium
      script:
      - echo "Threads/core $(nproc --all)"
      - free -g

language: ruby
os: linux
arch: ppc64le
dist: xenial
virt: lxd
addons:
  postgresql: 9.6
rvm: 2.6.3
cache: bundler
jobs:
  include:
    - stage: "rspec"
      script: bundle exec rspec
      before_script:
        - "bundle exec rake db:create db:migrate"
notifications:
  email:
    - shairyarbaig@gmail.com
